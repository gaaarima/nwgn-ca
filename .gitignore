.DS_Store
.claude/
*.log

# Raw full-resolution photo archive (2400px originals, ~584MB).
# Kept on disk but never committed — GitHub Pages only needs the
# web-optimised 1200px copies in gallery/. Back the originals up
# to Drive/Dropbox rather than to git.
Images/
